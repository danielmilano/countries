query Countries {
    countries {
        code
        name
        emoji
        native
        currency
        continent {
            name
        }
        languages {
            name
        }
    }
}